exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
       
        body: JSON.stringify('API has been invoked by Lennox'),
    };
    return response;
};
